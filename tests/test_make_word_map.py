from multiSourceWordMaps.makeWordMap import MapCreator
import unittest

class TestMakeWordMap(unittest.TestCase):

    def test_make_word_map(self):
        '''mock_config_editor.return_value = MockConfigEditor(
            {
                "package_dir":"test/path",
                "sources":[]
            }
        )

        extractor = Extractor(MockArgs(
            ticker="SQ",
            source="testFile.pdf"
        ))'''
        assert True

